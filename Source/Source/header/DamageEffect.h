#pragma once
#include <chrono>
/*
class DamageEffect {
private:
	std::chrono::time_point<std::chrono::steady_clock> lifeTimer;

public:
	DamageEffect(const int absPosLeft, const int absPosTop);
	void initTimer(void);
	bool isAffect(Marco marco);
	bool isAffect(Soldier soldier);
};
*/